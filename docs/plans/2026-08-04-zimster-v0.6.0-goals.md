# Zimster 0.6.0 Execution Goal

Use one Goal because routing, convergence, adapters, packages, and semantic completion share one compatibility boundary and one persistent owner.

```text
Use the canonical repository-local Zimster skills to implement the approved plan
at docs/plans/2026-08-04-zimster-v0.6.0.md.

Stay on feat/adaptive-delegation-v0.6.0. Do not create a branch or worktree.
Do not push, merge, tag, publish, create a pull request, or modify global user
configuration. Commit at the four verified vertical-slice boundaries defined
by the plan, with at most one consolidated correction commit.

The root task remains the one persistent implementation owner. Use no
implementation subagents and no nested delegation. Use one clean-context
independent reviewer identity for the required early routing/configuration seam
review and final integration review, with at most one resumed recheck per seam.

Freeze the checked-in Zimster 0.5.0 workflow as the authority for this
self-hosting run. Exercise unfinished 0.6.0 routing and convergence rules only
inside tests, temporary repositories, and isolated exact-package homes until
they pass their required review and acceptance evidence.

Implement the four slices in order:

1. Separate delegation decisions, advisory/authoritative proposals, mappings,
   dispatch resolution, and v1 compatibility.
2. Add policy-bounded routing, capability evidence, strict-cost behavior,
   owner acceptance, advisory empirical evidence, and six-harness adapters.
3. Add configurable bounded autonomous convergence and bootstrap safeguards.
4. Complete 0.6.0 public-beta documentation, versioning, generated mirror,
   packaging, all-six live installation/session smoke, and release evidence.

Preserve the invariants in the approved plan. Add failing tests before policy
changes, keep Node 22 compatibility, add no runtime dependency, and synchronize
plugins/zimster only from canonical source.

Before completion, inspect all repository state and run release verification,
checks, version agreement, mirror checks, doctor JSON, checksums, and diff
checks. Produce the semantic matrix, exact review package, postmortem, and
deterministic completion decision. Missing required live harness evidence must
yield BLOCKED_BY_ENVIRONMENT rather than a public-beta readiness claim.
```
